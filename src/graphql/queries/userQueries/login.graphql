mutation {
    login(
        LoginCredentials: {
            email: "admin@admin.com",
            password: "adminTest"
        }
    ) {
        userId
        firstName
        lastName
        email
        username
        createdAt
        updatedAt
        accountConfirmed
        forgotPasswordLocked
    }
}
